# Acknowledgements

The auto-generated Tier 1 list of every dependency the project pulls in: Rust crates from `Cargo.lock`, npm packages from `workspace/package.json`, and any binary blob shipped alongside the build. Each entry includes name, version, license (SPDX), and a one-line note on what it does for the project. This page is regenerated at every release; CI fails if it drifts from `NOTICE`. For the curated draft, see `CREDITS.md` at the repo root.

<!-- TODO: fill in during Phase 1 docs sprint -->
