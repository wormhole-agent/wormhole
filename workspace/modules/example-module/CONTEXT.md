# example-module: CONTEXT.md

> The agent loads this when the module activates. Be specific. Names, dates, decisions.

## What this module is for

(one paragraph: what is the project, who is it for, what counts as "done")

## Current state

(what's built, what's broken, what's blocked)

## Open loops

(things waiting on something. Use `LEDGER.md` if there are more than five.)

## Decisions made

- 2026-05-02 — Started this module from the example template. (Replace this line with real decisions.)

## Decisions to make

(things a future-you needs to decide; flag the constraints)

## Out of scope (deliberately)

(things the agent might assume are part of this module but are not)

## Skills and tools used

- See `skills/` and `tools/` in this directory.
- See workspace-level `tools/` and `skills/` for shared tooling.

## Memory tag

When working in this module, tag daily memory entries with `[example]` (or whatever callword you set in `INDEX.md`).
